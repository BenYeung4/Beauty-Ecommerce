    <div class="w-full md:w-1/2 object-center m-3 p-1 rounded-md bg-slate-100">
    <div class="float-right w-48"><img src="{{url}}" class="object-contain h-24 w-48" /></div>
    <div class="w-full mx-auto text-lg font-bold">{{name}}</div>
    <div class="w-full mx-auto text-sm">{{description}}</div>
    <div class="w-32"><label for="product-price">Price:{{product.price}}</label></div>
    <div class="w-full mx-auto text-sm">Weight/Volume: {{weight}}</div>
    <div class="w-full mx-auto float-right text-base">from {{manufacturer}}</div>
    <form class="add-cart">
    <div class="w-full mx-auto text-sm">
        <div class="float-right">Quantity: 
        <select id="quantity" name="quantity">
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
            <option value="5">5</option>
        </select>
        <button id="submit" class="bg-slate-900 hover:bg-slate-400 text-white px-3 py-2 rounded-md text-sm font-medium">Add to Cart</button>
        </div>
    </div>
    </form>
  </div>
  